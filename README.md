# VisualSorter
Simple program to visually compare sorting algorithms.

# Requirements
Linux, SDL2, make.

# Build
The c and cpp versions use the make file system. cd into the directory that you want and type make. The binary will be called visual_sorter_*.
